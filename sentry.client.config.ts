/**
 * DEPRECATED — moved to instrumentation-client.ts.
 *
 * Sentry's Next.js SDK now prefers `instrumentation-client.ts` at the
 * project root (matches the new official Next.js convention and works
 * under both Webpack and Turbopack). Active config lives there.
 *
 * This file is kept as a no-op stub so any tooling that looks for the
 * legacy filename doesn't error. Safe to delete entirely once you're
 * sure no script references it.
 */
export {};
